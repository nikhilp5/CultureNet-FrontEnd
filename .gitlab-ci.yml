image: node:latest

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - npm install
    - npm install netlify-cli -g
    - npm run build
    - netlify deploy --dir "build" --auth $AUTH_TOKEN_RISHI --site $SITE_ID_RISHI --prod
  only:
    - main-rishi